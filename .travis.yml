language: elixir

elixir:
  - 1.5
otp_release:
  - 20.1

sudo: false

before_script:
  - mix do deps.get, deps.compile
  - mix do ecto.create, ecto.migrate

script:
  - mix test --cover

env:
  - MIX_ENV=test

addons:
  postgresql: '9.4'

cache:
  directories:
    - _build
    - deps
